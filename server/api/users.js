// const router = require('express').Router();
// const { User, Frame, Session } = require('../db/models');

// router.get('/:userId', async (req, res, next) => {
//   try {
//     // const user = await User.FindByPk(req.params.userId);

//   } catch (error) {
//     next(error);
//   }
// })
